# seqedit
Interactive sequence editor.

Usage:

    seqedit [option] [fasta file]
    
    options
        -c: convert to complementary
        -t: sequence trimming

Requirement
    - BioPython package
